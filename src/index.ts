document.addEventListener("DOMContentLoaded", () => {
    const app = document.getElementById("app");
    if (app) {
        app.innerHTML = `<h1 class="text-3xl font-bold underline">Hello World from SvenCode.run.place!</h1>`;
    }
});